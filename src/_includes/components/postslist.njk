<div class="container px-5 py-24 mx-auto">
<div class="flex flex-col">
	  <div class="h-1 bg-black rounded overflow-hidden"></div>
	  <div class="flex flex-wrap sm:flex-row text-black flex-col py-6 mb-12">
		 <h1 class="sm:w-2/5 font-ibm_plex title-font text-2xl mb-2 sm:mb-0">Todo lo que necesitás saber</h1>
		 <p class="sm:w-3/5 leading-relaxed font-ibm_mono sm:pl-10 pl-0">Información sobre reservas, check-in, servicios y sobre las experiencias y paseos que recomendamos para la ciudad de Colonia y alrededores.</p>
	  </div>
	</div>
</div>
<div class="w-5/6 sm:w-1/2 mx-auto -my-24">
<ul class="container py-8 px-8 border-2 border-black rounded text-center">
  {% for post in postslist %}
    <li class="text-sm text-[#FEFC54] bg-black rounded-xl uppercase my-8 py-2">
      <strong>
        <a href="{{ post.url | url }}">{{ post.data.title }}</a>
      </strong>
      <span>{{ post.data.description }}</span>
    </li>
  {% endfor %}
</ul>
</div>
